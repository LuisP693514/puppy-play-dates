import './Filters.css';
// import { useDispatch, useSelector } from 'react-redux';
// import { useEffect } from 'react';

const Filters = () => {
    // const dispatch = useDispatch();
    // const filters = useSelector(getFilters);
    // const dogs = useSelector(getDogs); 
    //naming might change but the idea is to get all the dogs and then filter by tag 

    // useEffect(() => {
    //     dispatch(fetchDogs())
    //     dispatch(fetchFilters())
    // }, [dispatch]);

    // how to filter in the backend and how would that reflect in the map. would it just lead to different route or replotting of maps?

    return (
        // <select onChange={handleFilter}>
        //     <option key={all} value={'All'}>
        //         All
        //     </option>
        //     {filters.options.map((option, index) => (
        //         <option key={index} value={option}>
        //             {option}
        //         </option>
        //     ))}
        // </select>
        (<></>)
    );
};

export default Filters;